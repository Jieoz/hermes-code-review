# Hermes Code Review

A fail-closed Hermes Agent plugin for reviewing immutable staged Git candidates with one fixed independent model route.

The project is under active construction. Release usage and installation instructions are added only after the corresponding executable path is verified.
