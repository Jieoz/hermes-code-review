name: hermes-code-review
version: 0.2.0
description: Fail-closed immutable Git review with fixed, preapproved primary and infrastructure-fallback identities.
provides_tools:
  - review_git_candidate
  - code_review_status
